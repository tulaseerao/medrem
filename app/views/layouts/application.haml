/ !!!
/ %html{lang: "en"}
/   %head
/     %meta{charset: "utf-8"}/
/     %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}/
/     %title= content_for?(:title) ? yield(:title) : "foundation-rails"
/     = stylesheet_link_tag    "application"
/     = javascript_include_tag "vendor/modernizr"
/     = csrf_meta_tags
/   %body
/     %p.notice= notice
/     %p.alert= alert
/     = yield
/     = javascript_include_tag "application"
!!!
%html{lang: "en"}
  %head
    %meta{charset: "utf-8"}/
    %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}/
    %title= content_for?(:title) ? yield(:title) : "foundation-rails"
    = stylesheet_link_tag    "application"
    = csrf_meta_tags
  %body
    / %nav.top-bar
    /   %ul.title-area
    /     %li.name
    /       %h1= link_to "My Medicine Tracker", nil
    /   %section.top-bar-section
    /     %ul.right
    /       - if current_user
    /         %li.divider
    /         %li= link_to "My Dependants", nil
    /       %li.divider
    /       - if current_user
    /         %li= link_to "Logout", destroy_user_session_path, method: :delete
    /       - else
    /         %li= link_to "Login", new_user_session_path
    .off-canvas-wrap
      .inner-wrap
        %nav.tab-bar
          %section.left-small
            %a.left-off-canvas-toggle.menu-icon
              %span
          %section.middle.tab-bar-section
            %h1.title My Medicine Tracker
          %section.right-small
            %a.right-off-canvas-toggle.menu-icon
              %span
        %aside.left-off-canvas-menu
          %ul.off-canvas-list
            - if current_user
              %li= link_to "My Dependants", nil
              %li= link_to "My Medications", medications_path
              %li= link_to "Doctors", doctors_path
              %li= link_to "Pharmacies", pharmacies_path
        %aside.right-off-canvas-menu
          %ul.off-canvas-list
            - if current_user
              %li= link_to "Logout", destroy_user_session_path, method: :delete
            - else
              %li= link_to "Login", new_user_session_path
        %section.main-section
          = yield
          / content goes here
    %a.exit-off-canvas
    %p.notice= notice
    %p.alert= alert
    = javascript_include_tag "application"
    = javascript_include_tag "vendor/modernizr"

